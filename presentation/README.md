# Presentation

`slides.pdf` is the HarborLens submission deck.

Accepted formats are `slides.pdf` (preferred), `slides.pptx`, or `slides.key`. The deck covers the problem, solution, working prototype, IBM Bob integration, and impact.
