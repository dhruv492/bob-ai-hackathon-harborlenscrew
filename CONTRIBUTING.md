# Contribution and submission notes

Keep all application code inside `src/`. Do not commit secrets; use `src/.env.example` for variable names only. Before submission, replace the team identity placeholders in `submission.yaml`, record the demo video, and verify the app against `docs/setup-guide.md`.
