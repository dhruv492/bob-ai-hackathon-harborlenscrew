# Frontend

This folder contains the HarborLens browser application.

- `index.html` - accessible dashboard shell
- `styles.css` - responsive operational design system
- `app.js` - synthetic scenario data, explainable risk scoring, berth-plan generation, and briefing rendering

The frontend has no build step or external runtime dependency. The backend serves this directory during local development.
